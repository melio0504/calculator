// Get DOM here


// Main function and to add later more complicated stuffs
function add(x, y) {
    return x + y;    
}

function subtract(x, y) {
    return x - y;
}

function multiply(x, y) {
    return x * y;
}

function divide(x, y) {
    return x / y;
}
